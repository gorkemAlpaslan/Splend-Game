import React, { useState } from "react";
import Head from "next/head";
import MinesweeperSoloGame from "@/components/games/minesweeper-solo-game";
import style from "@/styles/Home.module.sass";

export default function MinesweeperSoloGamePage() {
  const [isPopupActive, setIsPopupActive] = useState<boolean>(false);

  const infoPopupHandler = () => {
    setIsPopupActive(!isPopupActive);
  };

  return (
    <div className={style.gameWrapper} id="game-page-wrapper">
      <Head>
        <title>Minesweeper Solo - Splend Game</title>
        <meta name="description" content="Identify mine coordinates and sweep all safe core sectors on the network." />
      </Head>

      <header className={style.gameHeader} style={{ background: "transparent", borderBottom: "none" }} id="game-header">
        <div className={style.logoTitle}>MINESWEEPER SOLO</div>
        <button className={style.infoButton} onClick={infoPopupHandler} id="btn-how-to-play">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="16" height="16" fill="currentColor">
            <path d="M12 22C6.477 22 2 17.523 2 12S6.477 2 12 2s10 4.477 10 10-4.477 10-10 10zm0-2a8 8 0 1 0 0-16 8 8 0 0 0 0 16zm-1-5h2v2h-2v-2zm2-1.645V14h-2v-1.5a1 1 0 0 1 1-1 1.5 1.5 0 1 0-1.5-1.5h-2a3.5 3.5 0 1 1 5 3.355z"/>
          </svg>
          How To Play
        </button>
      </header>

      <div style={{ display: "flex", justifyContent: "center", padding: "20px" }}>
        <MinesweeperSoloGame />
      </div>

      {isPopupActive && (
        <div className={style.infoModal} onClick={infoPopupHandler} id="rules-modal">
          <div
            className={style.infoContent}
            onClick={(e) => e.stopPropagation()}
          >
            <div className={style.modalHeader}>
              <span className={style.modalTitle}>HOW TO PLAY</span>
              <button className={style.closeButton} onClick={infoPopupHandler} id="btn-close-modal">×</button>
            </div>

            <div className={style.rulesList}>
              <div className={style.ruleItem}>
                <div className={style.ruleNum}>1</div>
                <div className={style.ruleText}><strong>Digging Cells:</strong> Click/tap a cell to uncover it. If the cell contains a hidden database mine, it detonate (loss).</div>
              </div>

              <div className={style.ruleItem}>
                <div className={style.ruleNum}>2</div>
                <div className={style.ruleText}><strong>Adjacency Indicators:</strong> Uncovering a safe cell displays a digit (1-8) revealing the exact count of mines in the surrounding 8 cells. Empty cells recursively reveal adjacent nodes.</div>
              </div>

              <div className={style.ruleItem}>
                <div className={style.ruleNum}>3</div>
                <div className={style.ruleText}><strong>Flagging Mines:</strong> Right-click a cell (or toggle <strong>Flag Mode</strong> on mobile) to mark a suspected mine coordinate with a flag 🚩, preventing accidental click triggers.</div>
              </div>

              <div className={style.ruleItem}>
                <div className={style.ruleNum}>4</div>
                <div className={style.ruleText}><strong>Victory Objective:</strong> Dig and reveal all safe sectors (non-mine cells). Once completed, the minefield is deactivated successfully.</div>
              </div>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
